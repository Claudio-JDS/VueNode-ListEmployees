{
  "name": "list-employees",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:front": "pnpm --filter frontend dev",
    "build:front": "pnpm --filter frontend build",
    "serve:front": "pnpm --filter frontend serve",
    "dev:back": "pnpm --filter backend start:server"
  },
  "devDependencies": {
    "pnpm": "^6.0.0"
  }
}
